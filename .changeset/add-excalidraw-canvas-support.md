---
'@inkeep/open-knowledge': patch
---

Add support for standalone `Excalidraw` drawing canvases when opening `.excalidraw`, `.canvas`, or `.okdraw` files. Canvas states are serialized to JSON and persisted directly in the file's collaborative CRDT state.
