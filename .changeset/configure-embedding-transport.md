---
'@inkeep/open-knowledge': patch
---

Configure semantic-search document batch size, character budget, and request timeout from `Settings → Search`, so slower local embedding servers can be tuned without patching the installed OpenKnowledge bundle.
