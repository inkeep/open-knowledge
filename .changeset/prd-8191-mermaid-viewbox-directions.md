---
"@inkeep/open-knowledge": patch
---

The arrow buttons on a Mermaid diagram now move the view in the direction the arrow points (matching GitHub) and each click eases into place instead of jumping. Respects `prefers-reduced-motion`.
