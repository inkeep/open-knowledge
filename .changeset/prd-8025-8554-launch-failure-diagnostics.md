---
"@inkeep/open-knowledge": patch
---

Bug reports now carry the output of in-app agent launches that failed, and a launch that fails on a stale npx cache entry clears the entry and retries once on its own.
