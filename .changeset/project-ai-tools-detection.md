---
'@inkeep/open-knowledge': patch
---

Project AI tools now puts integrations already wired or detected on this machine first and folds the rest behind a disclosure. Detection uses the machine's login-shell PATH and registered URL handlers, never MCP config folders, and failed probes quietly under-claim instead of guessing.
