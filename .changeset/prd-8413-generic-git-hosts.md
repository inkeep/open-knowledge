---
"@inkeep/open-knowledge": minor
---

Unknown git hosts now use your system credentials without GitHub permission checks blocking sync. For Enterprise sharing and sign-in, declare `git.hosts.<host>.provider: github` in `~/.ok/global.yml` and restart; see the [configuration guide](https://openknowledge.ai/docs/reference/configuration).
