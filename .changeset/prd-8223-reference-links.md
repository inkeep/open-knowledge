---
"@inkeep/open-knowledge": minor
---

Agent chat messages now link qualified GitHub references like `owner/repo#123`, and ticket prefixes you list under `autolinks` in `.ok/config.yml` turn references like `PRD-123` into links.
