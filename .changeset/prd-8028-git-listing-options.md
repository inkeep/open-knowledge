---
"@inkeep/open-knowledge": patch
---

Under the read-only command grant in agent chats, commands that only looked read-only now prompt, including `git branch` and `git tag` with anything but listing options, git formats that run a signature check, `printf`, abbreviated refused options, and arguments hidden behind a redirection.
