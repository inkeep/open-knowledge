---
"@inkeep/open-knowledge": patch
---

Collapse a sidebar folder from anywhere on its row, on the first click. Clicking a folder that was not the selected row re-expanded it and navigated instead of toggling, so a folder whose child document you had just opened took two clicks to close.
